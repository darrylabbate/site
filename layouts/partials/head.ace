meta name="viewport" content="charset=utf-8, initial-scale=1, width=device-width"
{{ $style := resources.Get "sass/style.sass" | toCSS | minify | fingerprint }}
link rel="stylesheet" href="{{ $style.Permalink }}"
